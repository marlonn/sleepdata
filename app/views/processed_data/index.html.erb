<%= render 'mode-form' %>

<% if    @mode == "table" %>
  <%= render 'table' %>
<% elsif @mode == "bar" %>
  <%= render 'scatter' %>
<% end %>

<br />

<%= link_to 'New Processed datum', new_processed_datum_path %>
